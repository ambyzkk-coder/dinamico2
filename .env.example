# Variabili d'ambiente per il database PostgreSQL
# Copia questo file in .env e compila i valori

# URL del database PostgreSQL (Render lo fornisce automaticamente)
DATABASE_URL=postgresql://user:password@host:port/database

# Ambiente (development o production)
NODE_ENV=production

# Porta del server (Render la fornisce automaticamente)
PORT=3000
